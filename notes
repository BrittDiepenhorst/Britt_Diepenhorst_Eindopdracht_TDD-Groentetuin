value A * value B = value C
value D * value E = value F 

C + F = result

crop.crop.yield * crop.numCrops = value 1
crop.crop.yield * crop.numCrops = value 2
1 + 2 = result 


Hoi Britt! Volgens mij slaat je vraag een deel van deze eindopdracht 
de spijker op de kop. Je wilt hier dus in de functie checks gaan doen om 
ervoor te zorgen dat de functie goed omgaat met verschillende inputs. 
(Hier zijn bijvoorbeeld if / else statements handig en switch statements).  
En je kan nadenken over welke inputs de functie nodig heeft om de correcte 
berekening te kunnen maken.

Zo heeft de getYieldForPlant functie naast “plant” ook environmentFactors nodig

Hier een stukje pseudocode:

const getYieldForPlant = (plant, environmentFactors) => {
  if (!environmentFactors) {
    return plant.yield;
  }
  let sun;
  let rain;
  let wind;
  if (!environmentFactors.sun) {
    sun = 1;
  } else {
    switch (environmentFactors.sun) {
      case "low":
         sun = berekening voor coefficient in lage zon conditie
         break;
      case "medium":
      sun = berekening voor coeffcient in medium zon conditie
       break; ...etc
    }
  }
  if (!environmentFactors.rain) {
    rain = 1;
  } else {
    switch (environmentFactors.rain) { 
  etc... dit voor de verschillende environmental factoren.
   
   ....
   ....
  Aan het einde van de functie een variabele en een return die dus de  yieldPerPlant omvat: const yieldPerPlant = plant.yield * zon *regen * wind
   return yieldPerPlant
}